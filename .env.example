# Database
DATABASE_URL="file:./dev.db"

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-change-this-in-production"

# Email (SMTP) for password reset emails
# Configure these to enable sending real emails. If not set, emails will be logged to the console.
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="username"
SMTP_PASS="password"
SMTP_FROM="SquadOdds <no-reply@squadodds.com>"

# Optional: Add other environment variables here
